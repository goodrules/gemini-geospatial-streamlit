---
description: Effective Logging
globs:
  - "**/*.py"  # Apply to all Python files
  - "**/*.pyi" # Apply to Python stub files
alwaysApply: true
---

Implement structured and informative logging using Python's standard `logging` module. Use appropriate log levels (DEBUG, INFO, WARNING, ERROR, CRITICAL). Avoid using `print()` statements for logging purposes in library or application code. Include relevant context in log messages.

