---
description: Pythonic Idioms
globs:
  - "**/*.py"  # Apply to all Python files
  - "**/*.pyi" # Apply to Python stub files
alwaysApply: true
---

Write idiomatic Python code. Favor list comprehensions, generator expressions, context managers (`with` statement), enumerate, zip, etc., over more verbose or C-style approaches when appropriate. Use Python's built-in functions and standard library effectively.

