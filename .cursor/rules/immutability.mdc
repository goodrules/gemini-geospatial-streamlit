---
description: Immutability
globs:
  - "**/*.py"  # Apply to all Python files
  - "**/*.pyi" # Apply to Python stub files
alwaysApply: true
---

Where appropriate, favor immutable data structures (like tuples) over mutable ones (like lists) for data that should not change after creation, especially when used as dictionary keys or in sets.

